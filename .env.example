# Copiar a .env (gitignored). service_role es SOLO server-side (§11).
SUPABASE_URL=http://127.0.0.1:55321
SUPABASE_SERVICE_ROLE_KEY=

# Solo durante un alta de tienda (scripts/onboard-shop.ts); no persisten aquí,
# el destino final de estos secretos es el Vault:
# SHOPIFY_ADMIN_TOKEN=shpat_...
# SHOPIFY_WEBHOOK_SECRET=...
